enum ACTION_STATUS {
  WAITING = 'waiting',
  RUNNING = 'running',
  STOPPED = 'stopped',
  PAUSED = 'paused',
  SUCCESS = 'success',
  ERROR = 'error',
  SKIPPED = 'skipped',
}

export { ACTION_STATUS }
