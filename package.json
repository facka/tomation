{
  "name": "tomation",
  "version": "1.5.2",
  "private": true,
  "workspaces": [
    "packages/dsl",
    "packages/compiler",
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run build --prefix packages/extension/panel-vue && npm run build --workspace=packages/extension",
    "build:chrome": "npm run build --prefix packages/extension/panel-vue && npm run build:chrome --workspace=packages/extension",
    "build:firefox": "npm run build --prefix packages/extension/panel-vue && npm run build:firefox --workspace=packages/extension",
    "playground": "npx live-server examples/playground",
    "compile:playground": "cd examples/playground-tests && npx tomation compile",
    "test": "npm test --workspaces",
    "test:compiler": "npm test --workspace=packages/compiler",
    "test:extension": "npm test --workspace=packages/extension",
    "release:minor": "./scripts/bump-and-release.sh minor",
    "release:major": "./scripts/bump-and-release.sh major",
    "release:patch": "./scripts/bump-and-release.sh patch"
  }
}
